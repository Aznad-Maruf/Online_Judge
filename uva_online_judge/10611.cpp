#include<bits/stdc++.h>
using namespace std;

long long loLady,hiLady;

int main(){
    long long n,q,num,temp;
    cin>>n;
    long long ara1[n+1];
    for(int a_i=1;a_i<=n;a_i++)cin>>ara[a_i];
    cin>>q;
    long long ara2[q+1];
    for(a_i=1;a_i<=q;a_i++)cin>>ara2[a_i];
}
